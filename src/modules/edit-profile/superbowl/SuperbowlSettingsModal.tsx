import {ModalProps} from '@chakra-ui/react';

type SuperbowlSettingsModalProps = Omit<ModalProps, 'children'>;

export function SuperbowlSettingsModal(props: SuperbowlSettingsModalProps) {
  return <></>;
}
