query Winners($league_id: Int!) {
  findManyWeekWinners(where: {league_id: {equals: $league_id}}) {
    week
    correct_count
    member {
      people {
        uid
        username
      }
    }
  }
}
