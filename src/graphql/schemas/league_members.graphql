query FindLeagueMembers($league_id: Int!) {
  leagueMembers(
    where: { league_id: { equals: $league_id } }
    orderBy: { people: { username: asc } }
  ) {
    membership_id
    people {
      uid
      username
    }
    leagues {
      name
    }
  }
}
